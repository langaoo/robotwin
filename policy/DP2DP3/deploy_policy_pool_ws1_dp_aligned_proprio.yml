# DP2DP3 Policy - pool_ws1 + DP-Aligned Proprio (n_obs_steps=3)
#
# 训练脚本: train_offline_head_dp_aligned.py
# 训练配置: train_offline_pool_ws1_dp_aligned_proprio.yaml
# Policy 类: DPAlignedPolicy (agent_pos 直接 concat，无 MLP)
# n_obs_steps=3, horizon=8, 完全对齐原版 DP

policy_name: null
task_name: null
task_config: null
ckpt_setting: null
seed: null
instruction_type: unseen

config_name: dp2dp3_4models
checkpoint_num: 600
expert_data_num: null
use_rgb: true

encoder:
  checkpoint: /home/gl/RoboTwin/policy/DP2DP3/features_model/outputs/train_rgb2pc_runs/run_pool_ws1/ckpt_final.pt
  freeze: true
  dummy_mode: none

policy:
  type: OfficialDP
  num_inference_steps: 100
  # DP-Aligned proprio: checkpoint 中已记录 policy_class=DPAlignedPolicy
  # deploy_policy.py 会自动检测并使用直接 concat 方式
  use_proprio: true
  proprio_dim: 14
